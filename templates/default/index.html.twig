{% extends 'base.html.twig' %}

{% block body %}
    <ul>
    {% for issue in issues %}
        <li><a href="{{ path('app_default_show', {id: issue.id}) }}">{{ issue.title }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}
